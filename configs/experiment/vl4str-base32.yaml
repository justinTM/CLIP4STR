# @package _global_
defaults:
  - override /model: vl4str

model:
  img_size: [ 224, 224 ]      # [ height, width ]
  patch_size: [ 32, 32 ]      # [ height, width ]

  # Training
  lr: 8.4e-5

  dec_num_heads: 8
  dec_mlp_ratio: 4
  dec_depth: 1

  clip_pretrained: checkpoints/pretrained/clip/clip4str_base_6e9fe947ac.pt